import { Router } from 'express'; import { crearOferta, listarOfertas, detalleOferta } from '../controllers/oferta.controller.js'; import { auth } from '../middlewares/auth.middleware.js'; import { authorize } from '../middlewares/authorize.middleware.js'; const r=Router(); r.get('/',listarOfertas); r.get('/:id',detalleOferta); r.post('/',auth,authorize(['Empresa']),crearOferta); export default r; 
